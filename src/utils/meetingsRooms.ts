// import { MeetingRoom } from "@/app/types/meetingRoom";

// export const getAllRooms = async (): Promise<MeetingRoom[]> => {
//   await new Promise((resolve) => setTimeout(resolve, 300));
//   return meetingRooms;
// };

// export const getRoomById = async (
//   id: string
// ): Promise<MeetingRoom | undefined> => {
//   await new Promise((resolve) => setTimeout(resolve, 300));
//   return meetingRooms.find((room) => room.id === id);
// };
